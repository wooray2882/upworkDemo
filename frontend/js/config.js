/**
 * Deployed backend configuration.
 * API_BASE_URL points at the live API Gateway from
 * infrastructure/environments/demo (see `terraform output api_endpoint`).
 */
window.APP_CONFIG = {
  API_BASE_URL: "https://f9iafbet89.execute-api.us-east-1.amazonaws.com"
};
